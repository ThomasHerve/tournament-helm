namespace: tournament-prod
ingress:
  enabled: true
image:
  repository: thomasherve/tournament-backend
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"